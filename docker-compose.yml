version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rosatida-back
    restart: unless-stopped
    ports:
      - '3000'
    env_file:
      - .env

